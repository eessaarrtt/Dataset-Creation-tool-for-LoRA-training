# Changelog

Все значимые изменения в проекте документируются в этом файле.

## [Версия 2.0] - 2024-12-07

### 🎉 Новые возможности

#### Поддержка NSFW контента
- **Разделение контента на категории**: Добавлена поддержка разделения контента на `normal/` и `nsfw/` папки в `Sample Dataset/`
- **Автоматическая настройка NSFW**: При выборе генерации NSFW контента автоматически настраиваются:
  - **Grok** для генерации промптов (NSFW контент)
  - **Seedream v4.5** для генерации изображений (NSFW контент)
  - **Grok** для генерации captions (NSFW контент)
- **Условная обработка**: Если NSFW контент отключен, файлы из папки `nsfw/` не обрабатываются

#### Интеграция Grok (xAI)
- **Grok как провайдер для промптов**: Добавлена поддержка Grok 4.1 Fast Reasoning для генерации промптов
- **Grok для captions**: Добавлена поддержка Grok для генерации captions с поддержкой NSFW контента
- **OpenAI-совместимый API**: Grok использует OpenAI-совместимый API для простой интеграции

#### Улучшения модели OpenAI
- **Единственная модель GPT-5.1**: Оставлена только одна модель OpenAI - GPT-5.1 для всех задач (промпты и captions)
- **Упрощенный выбор**: Убран выбор между GPT-5.1 и GPT-4o, теперь используется только GPT-5.1

#### Улучшения разрешений
- **Исправлены описания разрешений**: Обновлены описания разрешений в меню:
  - 1k: 1920×1920 (вместо 1024×1024) - минимум для Seedream моделей
  - 2k: 2048×2048
  - 4k: 4096×4096

### 🔧 Улучшения

#### Рефакторинг кода
- **Модульная структура**: Код разбит на модули в папке `src/`:
  - `config.py` - управление конфигурацией и профилями
  - `file_manager.py` - работа с локальными файлами
  - `prompt_generator.py` - генерация промптов
  - `image_generator.py` - генерация изображений
  - `caption_generator.py` - генерация captions
  - `dataset_creator.py` - оркестрация процесса создания датасета
  - `interactive_menu.py` - интерактивное меню
  - `utils.py` - вспомогательные функции
  - `main.py` - точка входа
- **Обратная совместимость**: Старый `dataset_creation.py` сохранен как обертка для обратной совместимости

#### Улучшения интерфейса
- **Улучшенное меню**: Более компактное и удобное меню с таблицами для NSFW/Normal настроек
- **Поток настройки**: Упрощенный поток - сначала вопрос о NSFW, затем настройка обычного контента
- **Итоговый отчет**: Улучшенный итоговый отчет с четким разделением настроек для NSFW и обычного контента

#### Локализация
- **Полная локализация**: Все сообщения в терминале теперь локализованы (русский/английский)
- **Исправлены ошибки перевода**: Исправлены все захардкоженные строки, теперь используются переводы из i18n

### 📝 Изменения в использовании

#### Новая структура папок
```
Sample Dataset/
├── normal/          # Обычный контент
│   └── *.jpg, *.png, ...
└── nsfw/            # NSFW контент (опционально)
    └── *.jpg, *.png, ...
```

#### Новый процесс настройки
1. Выбор профиля
2. **Вопрос о NSFW контенте** (новое!)
   - Если "Да": автоматически настраиваются Grok и Seedream v4.5 для NSFW
   - Если "Нет": все настройки применяются ко всему контенту
3. Настройка для обычного контента (или основных настроек)
4. Итоговый отчет

### ⚠️ Важные замечания

#### NSFW контент
- **Только Grok** поддерживает генерацию промптов и captions для NSFW контента
- **Только Seedream v4.5** поддерживает генерацию изображений для NSFW контента
- Если NSFW контент отключен (`nsfw_enabled = False`), файлы из папки `nsfw/` **не будут обрабатываться**

#### Миграция с предыдущих версий
- Старые профили будут работать, но рекомендуется пересоздать их для использования новых функций
- Если у вас были файлы в корне `Sample Dataset/`, они продолжат работать (обратная совместимость)
- Для использования NSFW функций создайте папку `nsfw/` в `Sample Dataset/`

### 🔑 Требования к API ключам

#### Новые ключи
- **Grok API ключ**: Требуется для использования Grok (получить на [xAI](https://x.ai))
  - Добавьте в `config.json`: `"grok_api_key": "ваш_ключ"`

#### Существующие ключи
- **OpenAI API ключ**: Теперь используется только для GPT-5.1
- **Gemini API ключ**: Без изменений
- **Wavespeed API ключ**: Без изменений

### 📚 Обновленная документация

- Обновлены `README_RU.md` и `README_EN.md` с информацией о новых функциях
- Добавлена информация о Grok и NSFW контенте
- Обновлены описания моделей

### 🐛 Исправления ошибок

- Исправлены ошибки локализации в терминале
- Исправлены описания разрешений (1k теперь правильно указан как 1920×1920)
- Исправлена логика обработки NSFW контента при отключенной опции

---

## Как обновиться

### Для новых пользователей
1. Клонируйте репозиторий или скачайте последнюю версию
2. Установите зависимости: `pip install -r requirements.txt`
3. Создайте `config.json` из `config.example.json`
4. Добавьте API ключи (включая Grok, если планируете использовать NSFW)

### Для существующих пользователей
1. Обновите код из репозитория
2. Установите новые зависимости (если есть): `pip install -r requirements.txt`
3. Добавьте `grok_api_key` в `config.json` (если планируете использовать Grok)
4. При необходимости пересоздайте профили для использования новых функций

### Использование новых функций

#### Работа с NSFW контентом
1. Создайте папку `nsfw/` в `Sample Dataset/`
2. Поместите NSFW изображения в эту папку
3. При запуске выберите "Да" на вопрос о генерации NSFW контента
4. Настройте обычный контент (для файлов из папки `normal/`)
5. NSFW контент будет автоматически обрабатываться с Grok и Seedream v4.5

#### Работа только с обычным контентом
1. Поместите изображения в папку `normal/` в `Sample Dataset/`
2. При запуске выберите "Нет" на вопрос о генерации NSFW контента
3. Настройте основные настройки (будут применяться ко всем файлам)

---

**Примечание**: Этот релиз включает значительные изменения в архитектуре и функциональности. Рекомендуется протестировать на небольшом наборе данных перед использованием в продакшене.

