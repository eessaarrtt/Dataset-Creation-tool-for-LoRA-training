# Dataset Creation - Создание датасетов для LoRA обучения

Полнофункциональный Python скрипт для создания датасетов изображений с автоматической генерацией промптов и подписей (captions) для обучения LoRA моделей.

## 📋 Содержание

1. [Описание проекта](#описание-проекта)
2. [Возможности](#возможности)
3. [Требования](#требования)
4. [Установка](#установка)
5. [Быстрый старт](#быстрый-старт)
6. [Подробная настройка](#подробная-настройка)
7. [Использование](#использование)
8. [Генерация подписей (Captions) для LoRA](#генерация-подписей-captions-для-lora)
9. [Система профилей](#система-профилей)
10. [Решение проблем](#решение-проблем)
12. [API ключи](#api-ключи)

---

## 📖 Описание проекта

Этот проект представляет собой Python-аналог Make.com workflow для автоматического создания датасетов изображений. Скрипт использует AI для генерации промптов, создает изображения через Wavespeed API и автоматически генерирует детальные подписи (captions) для обучения LoRA моделей.

### Основные компоненты:

- **Генерация промптов**: Использует Gemini, OpenAI или Grok для анализа референсных и sample изображений
- **Генерация изображений**: Создает новые изображения через Wavespeed API
- **Генерация captions**: Автоматически создает детальные подписи для LoRA обучения
- **Упаковка датасета**: Создает zip-архив с изображениями и подписями

---

## ✨ Возможности

### Основные функции

- ✅ **Локальное хранение файлов** (вместо облачных сервисов)
- ✅ **Выбор AI провайдера** для генерации промптов: **Gemini**, **OpenAI** или **Grok**
- ✅ **Генерация изображений** через **Wavespeed** API
- ✅ **Автоматическая обработка** множества изображений
- ✅ **Генерация captions** для LoRA обучения
- ✅ **Интерактивное меню** с навигацией стрелками и цветным выводом
- ✅ **Система профилей** для сохранения различных конфигураций
- ✅ **Мультиязычность** (русский и английский)
- ✅ **Красивый интерфейс** с цветным выводом и таблицами

### Поддерживаемые модели

**AI для промптов:**
- Gemini 2.5 Flash (быстрый, бесплатный)
- OpenAI GPT-5.1 (единственная модель OpenAI 5-й версии)
- Grok 4.1 Fast Reasoning (от xAI, поддерживает NSFW контент)

**Генерация изображений (Wavespeed):**
- Image-to-Image: `google/nano-banana-pro/edit`, `bytedance/seedream-v4.5/edit`, `bytedance/seedream-v4/edit`
  - Модели Seedream используют параметр `size` (например, "1024*1024", "2048*2048", "4096*4096")
  - Nano Banana Pro использует параметр `resolution` (1k, 2k, 4k)
- Image-to-Video: `alibaba/wan-2.5/image-to-video`, `kwaivgi/kling-v2.6-pro/image-to-video`, `kwaivgi/kling-v2.5-turbo-pro/image-to-video` ⚠️ **В разработке** - модули добавлены, но сценарии использования еще не реализованы. Будет доступно в следующих версиях.

**Генерация captions:**
- OpenAI GPT-5.1 (единственная модель OpenAI 5-й версии)
- Grok 4.1 Fast Reasoning (поддерживает NSFW контент)

**⚠️ Важно о NSFW контенте:**
- **Только Grok** поддерживает генерацию промптов и captions для NSFW контента
- **Только Seedream v4.5** поддерживает генерацию изображений для NSFW контента
- Для работы с NSFW контентом создайте папку `nsfw/` в `Sample Dataset/` и поместите туда соответствующие изображения
- Если NSFW контент отключен, файлы из папки `nsfw/` не будут обрабатываться

---

## 🔧 Требования

### Системные требования

**Программное обеспечение:**
- **Python 3.8+** (рекомендуется 3.10+)
  - Скачать с [python.org](https://www.python.org/downloads/)
  - При установке обязательно отметьте "Add Python to PATH" (Windows)
- **pip** (обычно идет вместе с Python)
  - Проверить установку: `pip --version`
- **Терминал/Командная строка**
  - Windows: Command Prompt или PowerShell
  - macOS/Linux: Terminal

**Аппаратное обеспечение:**
- Любой современный компьютер (специальных требований нет)
- Интернет соединение для API запросов
- Достаточно места на диске для изображений и результатов

**Аккаунты и ключи:**
- **API ключи** для выбранных провайдеров:
  - Ключ Gemini API (бесплатный, из Google AI Studio)
  - Ключ OpenAI API (платный, из OpenAI Platform)
  - Ключ Wavespeed API (платный, из Wavespeed.ai)

**Операционные системы:**
- ✅ Windows 10/11
- ✅ macOS 10.14+
- ✅ Linux (Ubuntu, Debian, Fedora и др.)

### Зависимости Python

Все зависимости указаны в `requirements.txt`:

```bash
# Основные зависимости
requests>=2.31.0
urllib3>=2.0.0

# AI провайдеры (выберите нужные)
google-generativeai>=0.3.0  # Для Gemini
openai>=1.0.0  # Для OpenAI

# Интерактивное меню
rich>=13.0.0  # Для красивого цветного вывода
inquirer>=3.1.0  # Для навигации стрелками в меню
```

---

## 🚀 Установка

### Шаг 1: Клонирование или загрузка проекта

```bash
# Если используете git
git clone <repository-url>
cd makenanalog

# Или просто скачайте и распакуйте архив
```

### Шаг 2: Проверка установки Python

Сначала убедитесь, что Python установлен правильно:

```bash
# Проверить версию Python
python --version
# или
python3 --version

# Проверить установку pip
pip --version
# или
pip3 --version
```

Если Python не установлен:
- **Windows/macOS**: Скачайте с [python.org](https://www.python.org/downloads/)
- **Linux**: Используйте менеджер пакетов:
  ```bash
  # Ubuntu/Debian
  sudo apt-get install python3 python3-pip
  
  # Fedora
  sudo dnf install python3 python3-pip
  ```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
# или
pip3 install -r requirements.txt
```

### Шаг 4: Создание конфигурации

```bash
cp config.example.json config.json
```

### Шаг 5: Настройка config.json

Откройте `config.json` и укажите:

1. **Пути к папкам** (можно оставить по умолчанию)
2. **API ключи** для выбранных провайдеров

```json
{
  "influencer_ref_folder": "./Influencer Reference Images",
  "sample_dataset_folder": "./Sample Dataset",
  "output_folder": "./output",
  "gemini_api_key": "YOUR_GEMINI_API_KEY_HERE",
  "openai_api_key": "YOUR_OPENAI_API_KEY_HERE",
  "wavespeed_api_key": "YOUR_WAVESPEED_API_KEY_HERE"
}
```

### Шаг 6: Подготовка папок

Создайте необходимые папки (или они создадутся автоматически):

```bash
mkdir -p "Influencer Reference Images"
mkdir -p "Sample Dataset"
mkdir -p "output"
```

---

## 🎯 Быстрый старт

### Минимальная настройка для первого запуска

1. **Проверьте установку Python:**
   ```bash
   python --version  # Должна быть версия 3.8 или выше
   pip --version     # Должна показать версию pip
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

3. **Создайте config.json:**
   ```bash
   cp config.example.json config.json
   ```

4. **Добавьте хотя бы один API ключ** в `config.json`:
   - Для Gemini: `gemini_api_key`
   - Для OpenAI: `openai_api_key`
   - Для Wavespeed: `wavespeed_api_key`

5. **Подготовьте изображения:**
   - Поместите **минимум 2 референсных изображения** в `Influencer Reference Images/`
   - Поместите **sample изображения** в `Sample Dataset/`

6. **Запустите скрипт:**
   ```bash
   python main.py
   # или
   python3 main.py
   ```
   
   **Примечание:** Для обратной совместимости также можно использовать `python dataset_creation.py`, но рекомендуется использовать `main.py`

При первом запуске появится **интерактивное меню** для выбора всех настроек!

---

## 📁 Структура проекта

Проект организован в модульную структуру для лучшей поддерживаемости:

```
makenanalog/
├── src/                          # Основной код приложения
│   ├── __init__.py              # Инициализация пакета
│   ├── config.py                # Управление конфигурацией и профилями
│   ├── file_manager.py          # Работа с файловой системой
│   ├── prompt_generator.py      # Генерация промптов (Gemini/OpenAI/Grok)
│   ├── image_generator.py       # Генерация изображений (Wavespeed)
│   ├── caption_generator.py     # Генерация captions для LoRA
│   ├── dataset_creator.py       # Оркестрация процесса создания датасета
│   ├── interactive_menu.py     # Интерактивное меню
│   └── utils.py                 # Вспомогательные функции
│
├── main.py                       # Точка входа приложения (рекомендуется)
├── dataset_creation.py           # Файл обратной совместимости
├── i18n.py                       # Система локализации
│
├── config.example.json          # Пример конфигурации
├── config.json                  # Основной конфигурационный файл
│
├── profiles/                     # Профили настроек
│   └── *.json
│
├── Sample Dataset/               # Примеры изображений
│   ├── normal/                  # Обычный контент
│   └── nsfw/                    # NSFW контент (опционально)
│
└── Influencer Reference Images/  # Референсные изображения
```

**Важно:**
- Для запуска используйте `main.py` (рекомендуется)
- `dataset_creation.py` сохранен для обратной совместимости
- Все основные модули находятся в папке `src/`

---

## ⚙️ Подробная настройка

### Структура config.json

```json
{
  "influencer_ref_folder": "./Influencer Reference Images",
  "sample_dataset_folder": "./Sample Dataset",
  "output_folder": "./output",
  "limit_ref_images": 10,
  "limit_sample_images": 10,
  "gemini_api_key": "ваш_ключ_gemini",
  "openai_api_key": "ваш_ключ_openai",
  "wavespeed_api_key": "ваш_ключ_wavespeed",
  "language": "ru"
}
```

### Параметры конфигурации

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `influencer_ref_folder` | Путь к папке с референсными изображениями | `./Influencer Reference Images` |
| `sample_dataset_folder` | Путь к папке с sample изображениями | `./Sample Dataset` |
| `output_folder` | Папка для сохранения результатов | `./output` |
| `limit_ref_images` | Максимальное количество референсных изображений | `10` |
| `limit_sample_images` | Максимальное количество sample изображений | `10` |
| `gemini_api_key` | API ключ для Gemini | - |
| `openai_api_key` | API ключ для OpenAI | - |
| `wavespeed_api_key` | API ключ для Wavespeed | - |
| `language` | Язык интерфейса (`ru` или `en`) | `ru` |

---

## 💻 Как запустить скрипт

### Способ 1: Терминал / Командная строка (рекомендуется)

**Windows:**
1. Откройте **Command Prompt** (cmd) или **PowerShell**
   - Нажмите `Win + R`, введите `cmd` или `powershell`, нажмите Enter
2. Перейдите в папку проекта:
   ```bash
   cd C:\путь\к\makenanalog
   ```
3. Запустите скрипт:
   ```bash
   python main.py
   ```

**macOS / Linux:**
1. Откройте **Terminal**
   - macOS: Нажмите `Cmd + Space`, введите "Terminal", нажмите Enter
   - Linux: Нажмите `Ctrl + Alt + T` или найдите Terminal в приложениях
2. Перейдите в папку проекта:
   ```bash
   cd /путь/к/makenanalog
   ```
3. Запустите скрипт:
   ```bash
   python main.py
   # или
   python3 main.py
   ```

### Способ 2: IDE (Интегрированная среда разработки)

Вы можете запустить скрипт из популярных редакторов кода:

**Visual Studio Code:**
1. Откройте VS Code
2. File → Open Folder → Выберите папку проекта
3. Откройте `main.py`
4. Правый клик → "Run Python File in Terminal"
   - Или нажмите `F5` и выберите "Python File"

**PyCharm:**
1. Откройте PyCharm
2. File → Open → Выберите папку проекта
3. Правый клик на `main.py` → "Run 'main'"
   - Или нажмите `Shift + F10`

**IDLE (встроенная IDE Python):**
1. Откройте IDLE
2. File → Open → Выберите `main.py`
3. Run → Run Module (или нажмите `F5`)

### Способ 3: Создание файла-ярлыка

**Windows - Создайте `run.bat`:**
1. Создайте новый текстовый файл с именем `run.bat` в папке проекта
2. Добавьте это содержимое:
   ```batch
   @echo off
   cd /d "%~dp0"
   python main.py
   pause
   ```
3. Сохраните и запустите двойным кликом

**macOS / Linux - Создайте `run.sh`:**
1. Создайте новый файл с именем `run.sh` в папке проекта
2. Добавьте это содержимое:
   ```bash
   #!/bin/bash
   cd "$(dirname "$0")"
   python3 main.py
   ```
3. Сделайте его исполняемым:
   ```bash
   chmod +x run.sh
   ```
4. Запустите двойным кликом или командой: `./run.sh`

### Способ 4: Jupyter Notebook (для продвинутых)

Если вы хотите запускать части скрипта интерактивно:
1. Установите Jupyter: `pip install jupyter`
2. Создайте notebook: `jupyter notebook`
3. Импортируйте и используйте функции из модулей в папке `src/`

---

## 💻 Использование

### Интерактивный режим (рекомендуется)

```bash
python main.py
```

**Примечание:** Для обратной совместимости также можно использовать `python dataset_creation.py`, но рекомендуется использовать `main.py`

При запуске появится интерактивное меню с:
- **Навигацией стрелками** (если установлен `inquirer`)
- **Цветным выводом** с таблицами и панелями (если установлен `rich`)
- **Режимом fallback** с простым текстовым вводом, если библиотеки не установлены

Пример:
```
╔════════════════════════════════════════════════════════╗
║     🎨 Dataset Creation - Интерактивная настройка     ║
╚════════════════════════════════════════════════════════╝

1️⃣  AI провайдер для генерации промптов:
   Текущее значение: gemini
   
   [1] Gemini
   [2] OpenAI
   
   Ваш выбор: [Используйте стрелки для навигации]
```

### 📊 Схемы всех возможных исходов интерактивного меню

#### Основной поток меню

```
┌─────────────────────────────────────────────────────────┐
│ 1. Выбор профиля                                        │
│    ├─ [1-N] Выбрать существующий профиль               │
│    ├─ [0] Создать новый профиль                        │
│    └─ [Enter] Пропустить (временные настройки)         │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 2. Вопрос о NSFW контенте                               │
│    ├─ [1] Да → Автоматически:                          │
│    │      • Grok для NSFW промптов                     │
│    │      • Seedream v4.5 для NSFW генерации           │
│    │      • Grok для NSFW captions                     │
│    │      → Переход к настройке обычного контента      │
│    │                                                    │
│    ├─ [2] Нет → Основные настройки применяются ко всему │
│    │                                                    │
│    └─ [Enter] Пропустить → = "Нет"                     │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 3. AI провайдер для промптов                            │
│    (Если NSFW=Да: для обычного контента)                │
│    (Если NSFW=Нет: для всего контента)                 │
│    ├─ [1] Gemini                                        │
│    ├─ [2] OpenAI (GPT-5.1)                              │
│    ├─ [3] Grok                                          │
│    └─ [Enter] Использовать из config/профиля            │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 4. Режим обработки                                      │
│    ├─ [1] bulk - Массовая обработка                    │
│    ├─ [2] detailed - Одно изображение                  │
│    └─ [Enter] Использовать из config/профиля            │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 5. Провайдер генерации изображений                     │
│    (Если NSFW=Да: для обычного контента)                │
│    ├─ [1] Wavespeed                                     │
│    └─ [Enter] Использовать из config/профиля           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 6. Модель Wavespeed                                     │
│    (Если NSFW=Да: для обычного контента)                │
│    ├─ [1] nano-banana-pro/edit                          │
│    ├─ [2] seedream-v4.5/edit                            │
│    ├─ [3] seedream-v4/edit                              │
│    ├─ [4-6] Video модели (⚠️ В разработке)             │
│    └─ [Enter] Использовать из config/профиля           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 7. Разрешение                                           │
│    ├─ [1] 1k (1920×1920)                                │
│    ├─ [2] 2k (2048×2048)                                │
│    ├─ [3] 4k (4096×4096)                                │
│    └─ [Enter] Использовать из config/профиля           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 8. Генерация captions                                   │
│    (Если NSFW=Да: для обычного контента)                │
│    ├─ [1] Да → Переход к шагу 9                        │
│    ├─ [2] Нет → Пропустить captions                     │
│    └─ [Enter] Использовать из config/профиля            │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 9. Trigger name (только если captions=Да)               │
│    └─ Ввод имени персонажа (одно слово)                │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 10. Провайдер для captions (только если captions=Да)    │
│     ├─ [1] OpenAI → Автоматически GPT-5.1              │
│     ├─ [2] Grok → Автоматически grok-4-1-fast-reasoning│
│     └─ [Enter] Использовать из config/профиля           │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 11. Итоговый отчет                                      │
│     └─ Показывает все выбранные настройки               │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ 12. Сохранение профиля                                  │
│     ├─ [1] Да → Ввод имени профиля → Сохранение         │
│     └─ [2] Нет → Пропустить                             │
└─────────────────────────────────────────────────────────┘
```

#### Сценарий 1: NSFW контент включен

```
NSFW = Да
  │
  ├─ Автоматические настройки для NSFW:
  │   • AI провайдер: Grok
  │   • Модель промптов: grok-4-1-fast-reasoning
  │   • Wavespeed модель: seedream-v4.5/edit
  │   • Caption провайдер: Grok
  │   • Caption модель: grok-4-1-fast-reasoning
  │
  └─ Настройки для обычного контента:
      ├─ AI провайдер: [Выбор: Gemini/OpenAI/Grok]
      ├─ Режим: [Выбор: bulk/detailed]
      ├─ Wavespeed модель: [Выбор: nano-banana/seedream-v4/seedream-v4.5]
      ├─ Разрешение: [Выбор: 1k/2k/4k]
      ├─ Captions: [Выбор: Да/Нет]
      │   └─ Если Да:
      │       ├─ Trigger name: [Ввод]
      │       └─ Caption провайдер: [Выбор: OpenAI/Grok]
      │           ├─ OpenAI → GPT-5.1
      │           └─ Grok → grok-4-1-fast-reasoning
      └─ Итог: Разные настройки для NSFW и normal
```

#### Сценарий 2: NSFW контент отключен

```
NSFW = Нет (или Enter)
  │
  └─ Основные настройки (применяются ко всему):
      ├─ AI провайдер: [Выбор: Gemini/OpenAI/Grok]
      ├─ Режим: [Выбор: bulk/detailed]
      ├─ Wavespeed модель: [Выбор: nano-banana/seedream-v4/seedream-v4.5]
      ├─ Разрешение: [Выбор: 1k/2k/4k]
      ├─ Captions: [Выбор: Да/Нет]
      │   └─ Если Да:
      │       ├─ Trigger name: [Ввод]
      │       └─ Caption провайдер: [Выбор: OpenAI/Grok]
      │           ├─ OpenAI → GPT-5.1
      │           └─ Grok → grok-4-1-fast-reasoning
      └─ Итог: Одинаковые настройки для всего контента
```

#### Матрица возможных комбинаций

| NSFW | AI Провайдер | Wavespeed Модель | Captions | Caption Провайдер | Результат |
|------|--------------|------------------|----------|-------------------|-----------|
| Да   | Gemini       | nano-banana      | Да       | OpenAI            | NSFW: Grok+Seedream v4.5, Normal: Gemini+nano-banana+OpenAI |
| Да   | OpenAI       | seedream-v4      | Да       | Grok              | NSFW: Grok+Seedream v4.5, Normal: OpenAI+seedream-v4+Grok |
| Да   | Grok         | seedream-v4.5    | Нет      | -                 | NSFW: Grok+Seedream v4.5, Normal: Grok+seedream-v4.5 |
| Нет  | Gemini       | nano-banana      | Да       | OpenAI            | Все: Gemini+nano-banana+OpenAI |
| Нет  | OpenAI       | seedream-v4      | Да       | Grok              | Все: OpenAI+seedream-v4+Grok |
| Нет  | Grok         | seedream-v4.5    | Нет      | -                 | Все: Grok+seedream-v4.5 |

#### Важные моменты

1. **Автоматические настройки NSFW** (если NSFW=Да):
   - Нельзя изменить через меню
   - Всегда: Grok + Seedream v4.5 + Grok для captions

2. **Пропуск шагов** (Enter):
   - Используются значения из config.json или профиля
   - Если значения нет → используется значение по умолчанию

3. **Обязательные шаги**:
   - Провайдер генерации изображений (Wavespeed) - обязательно выбрать
   - Trigger name - обязательно, если captions=Да

4. **Обработка файлов**:
   - Если NSFW=Да: файлы из `nsfw/` обрабатываются с NSFW настройками
   - Если NSFW=Нет: файлы из `nsfw/` **не обрабатываются**
   - Файлы из `normal/` всегда обрабатываются с настройками для normal контента

---

### Режим без интерактивного меню

```bash
python main.py --no-interactive
```

Использует только настройки из `config.json` и аргументы командной строки.

### С аргументами командной строки

```bash
# Использовать OpenAI вместо Gemini
python main.py --ai-provider openai --no-interactive

# Использовать конкретный профиль
python main.py --profile my_profile --no-interactive

# Указать другой файл конфигурации
python main.py --config my_config.json
```

### Полный список аргументов

```bash
python main.py --help
```

---

## 📝 Генерация подписей (Captions) для LoRA

### Что такое captions для LoRA?

Captions (подписи) - это текстовые описания изображений, которые используются для обучения LoRA моделей. Чем детальнее описание, тем лучше модель "запомнит" характеристики персонажа.

### Как работает генерация captions

1. **После генерации изображений** скрипт предлагает создать captions
2. **Укажите trigger name** (имя триггера) - это ключевое слово, которое будет использоваться в каждой подписи
3. **OpenAI анализирует изображение** и создает детальное описание
4. **Создаются файлы:**
   - `trigger_name_0001.txt` - подпись для первого изображения
   - `trigger_name_0002.txt` - подпись для второго изображения
   - и т.д.
5. **Изображения переименовываются:**
   - `trigger_name_0001.png`
   - `trigger_name_0002.png`
   - и т.д.
6. **Создается zip-архив** `trigger_name_lora_dataset.zip` со всеми файлами

### Пример подписи

**Плохо:**
```
Elara
```

**Хорошо:**
```
Elara with blonde hair, wearing a blue dress, standing in a garden, sunny day, detailed face, full body portrait
```

### Настройка генерации captions

В интерактивном меню вы можете выбрать:

1. **Генерировать ли captions** (Да/Нет)
2. **Trigger name** (имя триггера)
3. **Модель OpenAI** для генерации captions:
   - GPT-5.1 (рекомендуется, лучшее качество, поддерживает vision)
   - GPT-4o (проверенная модель, поддерживает vision)
   
   **⚠️ Примечание:** GPT-5 Nano и GPT-5 Mini не поддерживают анализ изображений и не могут использоваться для captions.

### Структура папки lora_dataset

```
output/
└── lora_dataset/
    ├── trigger_name_0001.png
    ├── trigger_name_0001.txt
    ├── trigger_name_0002.png
    ├── trigger_name_0002.txt
    └── ...
```

И zip-архив:
```
output/
└── trigger_name_lora_dataset.zip
```

---

## 👤 Система профилей

Профили позволяют сохранять различные конфигурации для разных задач.

### Создание профиля

1. Запустите скрипт в интерактивном режиме
2. Выберите все нужные настройки
3. В конце скрипт предложит сохранить настройки как профиль
4. Введите имя профиля (например, `production`, `testing`, `character1`)

### Использование профиля

```bash
# Использовать конкретный профиль
python main.py --profile production

# Показать список всех профилей
python main.py --list-profiles
```

### Где хранятся профили

Профили сохраняются в папке `profiles/`:

```
profiles/
├── production.json
├── testing.json
└── character1.json
```

### Что сохраняется в профиле

- AI провайдер и модель
- Провайдер генерации изображений и модель
- Шаблон промпта (bulk/detailed)
- Разрешение и формат вывода
- Настройки генерации captions
- Trigger name

**Не сохраняется в профиле:**
- API ключи (хранятся в `config.json`)
- Пути к папкам (хранятся в `config.json`)

---

## 🔍 Решение проблем

### Ошибка "API ключ не установлен"

**Проблема:** Скрипт не может найти API ключ для выбранного провайдера.

**Решение:**
1. Проверьте, что вы заполнили соответствующий ключ в `config.json`
2. Убедитесь, что файл `config.json` существует
3. Проверьте правильность ключа (нет лишних пробелов, кавычек)

### Ошибка "Папка не найдена"

**Проблема:** Скрипт не может найти папку с изображениями.

**Решение:**
1. Создайте папки `Influencer Reference Images` и `Sample Dataset`
2. Проверьте пути в `config.json`
3. Убедитесь, что в папках есть изображения (минимум 2 в референсной папке)

### Ошибка импорта библиотек

**Проблема:** Python не может найти необходимые библиотеки.

**Решение:**
```bash
# Установите все зависимости
pip install -r requirements.txt

# Или установите конкретные библиотеки
pip install google-generativeai  # Для Gemini
pip install openai  # Для OpenAI
pip install requests  # Для HTTP запросов
```

### Ошибка при генерации изображения

**Проблема:** Wavespeed API вернул ошибку.

**Решение:**
1. Проверьте баланс API ключа Wavespeed
2. Убедитесь, что модель указана правильно
3. Проверьте интернет соединение
4. Посмотрите файлы `*_response.json` в папке output для деталей ошибки

### Ошибка "Модель не поддерживается"

**Проблема:** Выбранная модель не поддерживает нужную функцию.

**Решение:**
- Для image-to-image используйте: `google/nano-banana-pro/edit`, `bytedance/seedream-v4.5/edit`, `bytedance/seedream-v4/edit`
- ⚠️ **Image-to-Video модели**: Модули для генерации видео из изображений добавлены в скрипт, но полные сценарии использования еще не реализованы. Эти функции будут доступны в следующих версиях проекта. Пожалуйста, не используйте Image-to-Video модели до официального релиза этой функциональности.

### Файлы перезаписываются

**Проблема:** При повторном запуске файлы с одинаковыми именами перезаписываются.

**Решение:** Скрипт автоматически добавляет временные метки к именам файлов, чтобы избежать перезаписи. Если вы видите перезапись, проверьте логи.

---

## 🔑 API ключи

### Получение API ключей

#### Gemini

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в аккаунт Google
3. Нажмите "Create API Key"
4. Скопируйте ключ и добавьте в `config.json`:
   ```json
   "gemini_api_key": "ваш_ключ_здесь"
   ```

**Особенности:**
- Бесплатный с лимитами (60 запросов/мин)
- Быстрый ответ
- Хорошее качество промптов

#### OpenAI

1. Перейдите на [OpenAI Platform](https://platform.openai.com/api-keys)
2. Войдите в аккаунт
3. Нажмите "Create new secret key"
4. Скопируйте ключ и добавьте в `config.json`:
   ```json
   "openai_api_key": "ваш_ключ_здесь"
   ```

**Особенности:**
- Платный (но есть бесплатный кредит для новых пользователей)
- Высокое качество
- Поддержка GPT-5 моделей

**Доступные модели для captions (с поддержкой vision):**
- GPT-5.1: $1.250 / 1M input, $10.000 / 1M output (рекомендуется для captions)
- GPT-4o: $2.50 / 1M input, $10.00 / 1M output (проверенная модель)

**⚠️ Важно:** GPT-5 Nano и GPT-5 Mini не поддерживают анализ изображений (vision API) и не могут использоваться для генерации captions.

#### Wavespeed

1. Перейдите на [Wavespeed](https://wavespeed.ai)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел API Keys
4. Создайте новый ключ
5. Добавьте в `config.json`:
   ```json
   "wavespeed_api_key": "ваш_ключ_здесь"
   ```

**Особенности:**
- Платный сервис
- Множество моделей для разных задач
- Высокое качество генерации

**Цены моделей:**
- Nano Banana Pro Edit: $0.14 (1k/2k), $0.24 (4k)
- Seedream v4.5: $0.04 за изображение (любое разрешение)
- Seedream v4: $0.027 за изображение (любое разрешение)
- Video модели: зависит от длительности

### Безопасность API ключей

⚠️ **Важно:**
- Никогда не публикуйте `config.json` с реальными ключами
- Добавьте `config.json` в `.gitignore`
- Используйте `config.example.json` для шаблона
- Регулярно проверяйте использование ключей
- При утечке ключа - немедленно отзовите его и создайте новый

---

---

## 📄 Лицензия

Используйте свободно для ваших проектов.

---

## 🤝 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Изучите дополнительную документацию
3. Проверьте логи в консоли
4. Проверьте файлы `*_response.json` в папке output

---

**Удачной работы с созданием датасетов! 🚀**

