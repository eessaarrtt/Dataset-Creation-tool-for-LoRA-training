# 📖 Пошаговая инструкция по использованию Dataset Creation Tool

Полное руководство по работе с инструментом для создания датасетов для обучения LoRA моделей.

---

## 🎯 Что делает этот инструмент?

Этот инструмент автоматизирует создание датасетов изображений для обучения LoRA моделей:

1. **Анализирует** ваши референсные и sample изображения с помощью AI
2. **Генерирует** детальные промпты для создания новых изображений
3. **Создает** новые изображения через Wavespeed API
4. **Генерирует** подписи (captions) для каждого изображения
5. **Упаковывает** всё в готовый датасет для обучения LoRA

---

## 📋 Шаг 1: Подготовка окружения

### 1.1 Установка Python

**Windows:**
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно** отметьте галочку "Add Python to PATH"
3. Проверьте установку: откройте командную строку и введите `python --version`

**macOS:**
1. Python обычно уже установлен. Проверьте: `python3 --version`
2. Если нет, установите через Homebrew: `brew install python3`

**Linux:**
```bash
# Ubuntu/Debian
sudo apt-get install python3 python3-pip

# Fedora
sudo dnf install python3 python3-pip
```

### 1.2 Установка зависимостей

1. Откройте терминал/командную строку
2. Перейдите в папку проекта:
   ```bash
   cd путь/к/папке/makenanalog
   ```
3. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
   или
   ```bash
   pip3 install -r requirements.txt
   ```

---

## 🔑 Шаг 2: Получение API ключей

### 2.1 Gemini API (бесплатный, для промптов)

1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Войдите в аккаунт Google
3. Нажмите "Create API Key"
4. Скопируйте ключ (он понадобится в следующем шаге)

**Особенности:**
- ✅ Бесплатный (с лимитами: 60 запросов/мин)
- ✅ Быстрая генерация промптов
- ❌ Не поддерживает NSFW контент

### 2.2 OpenAI API (платный, для промптов и captions)

1. Перейдите на [OpenAI Platform](https://platform.openai.com/api-keys)
2. Войдите в аккаунт
3. Нажмите "Create new secret key"
4. Скопируйте ключ

**Особенности:**
- 💰 Платный (есть бесплатный кредит для новых пользователей)
- ✅ Высокое качество
- ✅ Поддерживает GPT-5.1
- ❌ Не поддерживает NSFW контент

### 2.3 Grok API (обязателен для NSFW контента)

1. Перейдите на [xAI Console](https://console.x.ai/)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys" или "Settings"
4. Создайте новый API ключ
5. Скопируйте ключ

**Особенности:**
- 💰 Платный
- ✅ **Единственный провайдер, поддерживающий NSFW контент**
- ✅ Используется для промптов и captions для NSFW изображений
- ⚠️ **Обязателен**, если планируете работать с NSFW контентом

### 2.4 Wavespeed API (для генерации изображений)

1. Перейдите на [Wavespeed](https://wavespeed.ai)
2. Зарегистрируйтесь или войдите
3. Перейдите в раздел "API Keys"
4. Создайте новый ключ
5. Скопируйте ключ

**Особенности:**
- 💰 Платный сервис
- ✅ Множество моделей для разных задач
- ✅ Высокое качество генерации

**Цены моделей:**
- Nano Banana Pro Edit: $0.14 (1k/2k), $0.24 (4k)
- Seedream v4.5: $0.04 за изображение (любое разрешение)
- Seedream v4: $0.027 за изображение (любое разрешение)

---

## ⚙️ Шаг 3: Настройка конфигурации

### 3.1 Создание config.json

1. Скопируйте пример конфигурации:
   ```bash
   cp config.example.json config.json
   ```

2. Откройте `config.json` в любом текстовом редакторе

### 3.2 Заполнение API ключей

Добавьте полученные API ключи в `config.json`:

```json
{
  "influencer_ref_folder": "./Influencer Reference Images",
  "sample_dataset_folder": "./Sample Dataset",
  "output_folder": "./output",
  "gemini_api_key": "ваш_ключ_gemini_здесь",
  "openai_api_key": "ваш_ключ_openai_здесь",
  "grok_api_key": "ваш_ключ_grok_здесь",
  "wavespeed_api_key": "ваш_ключ_wavespeed_здесь"
}
```

**Важно:**
- Замените `ваш_ключ_..._здесь` на реальные ключи
- Не публикуйте `config.json` с ключами в открытом доступе
- `grok_api_key` нужен только если планируете работать с NSFW контентом

### 3.3 Настройка путей (опционально)

Если хотите использовать другие папки, измените пути:

```json
{
  "influencer_ref_folder": "./путь/к/референсным/изображениям",
  "sample_dataset_folder": "./путь/к/sample/изображениям",
  "output_folder": "./путь/к/результатам"
}
```

---

## 📁 Шаг 4: Подготовка изображений

### 4.1 Референсные изображения

1. Создайте папку `Influencer Reference Images` (или используйте путь из config.json)
2. Поместите туда **минимум 2 изображения** лица персонажа
   - Формат: JPG, PNG, GIF, BMP, WebP
   - Рекомендуется: четкие фотографии лица, разные ракурсы
   - Эти изображения используются для сохранения идентичности персонажа

**Пример структуры:**
```
Influencer Reference Images/
├── face_1.jpg
├── face_2.png
└── face_3.jpg
```

### 4.2 Sample изображения (обычный контент)

1. Создайте папку `Sample Dataset/normal/`
2. Поместите туда изображения с полными сценами
   - Это изображения, которые вы хотите "перерисовать" с лицом вашего персонажа
   - Формат: JPG, PNG, GIF, BMP, WebP

**Пример структуры:**
```
Sample Dataset/
└── normal/
    ├── scene_1.jpg
    ├── scene_2.png
    └── scene_3.jpg
```

### 4.3 Sample изображения (NSFW контент, опционально)

**⚠️ Только если планируете работать с NSFW контентом:**

1. Создайте папку `Sample Dataset/nsfw/`
2. Поместите туда NSFW изображения
3. **Обязательно** получите Grok API ключ (см. Шаг 2.3)

**Пример структуры:**
```
Sample Dataset/
├── normal/
│   └── scene_1.jpg
└── nsfw/
    ├── nsfw_1.jpg
    └── nsfw_2.jpg
```

**Важно:**
- NSFW контент обрабатывается только если вы включите эту опцию в меню
- Без Grok API ключа NSFW контент обрабатываться не будет

---

## 🚀 Шаг 5: Запуск скрипта

### 5.1 Базовый запуск

1. Откройте терминал/командную строку
2. Перейдите в папку проекта:
   ```bash
   cd путь/к/папке/makenanalog
   ```
3. Запустите скрипт:
   ```bash
   python main.py
   ```
   или
   ```bash
   python3 main.py
   ```

### 5.2 Что происходит при запуске

1. **Выбор языка** (русский/английский)
2. **Выбор профиля** (или создание нового)
3. **Интерактивное меню** для настройки всех параметров
4. **Обработка изображений**
5. **Создание датасета**

---

## 🎨 Шаг 6: Работа с интерактивным меню

### 6.1 Выбор профиля

При первом запуске вы увидите меню выбора профиля:

```
[1] Существующий профиль
[0] Создать новый профиль
[Enter] Пропустить (временные настройки)
```

**Рекомендация:** Создайте профиль для сохранения настроек.

### 6.2 Настройка NSFW контента

**Вопрос:** "Будете ли вы генерировать NSFW контент?"

- **[1] Да** → Автоматически настраиваются:
  - Grok для NSFW промптов
  - Seedream v4.5 для NSFW генерации
  - Grok для NSFW captions
  - Затем настраивается обычный контент отдельно

- **[2] Нет** → Основные настройки применяются ко всему контенту

**Важно:** Если выберете "Нет", файлы из папки `nsfw/` обрабатываться не будут.

### 6.3 Выбор AI провайдера для промптов

**Для обычного контента (или для всего, если NSFW отключен):**

- **[1] Gemini** - быстрый, бесплатный
- **[2] OpenAI** - высокое качество, платный
- **[3] Grok** - поддерживает NSFW, платный

**Рекомендации:**
- Для обычного контента: Gemini (бесплатно) или OpenAI (качество)
- Для NSFW контента: автоматически выберется Grok

### 6.4 Выбор режима обработки

- **[1] bulk** - массовая обработка (обрабатывает много изображений за раз)
- **[2] detailed** - обработка одного изображения (интерактивный выбор)

**Рекомендация:** Используйте `bulk` для создания датасета.

### 6.5 Выбор модели Wavespeed

**Для обычного контента:**

- **[1] nano-banana-pro/edit** - $0.14 (1k/2k), $0.24 (4k)
- **[2] seedream-v4.5/edit** - $0.04 за изображение
- **[3] seedream-v4/edit** - $0.027 за изображение

**Для NSFW контента:** Автоматически выберется Seedream v4.5

**Рекомендации:**
- Для обычного контента: nano-banana (качество) или seedream-v4 (дешевле)
- Для NSFW: только Seedream v4.5

### 6.6 Выбор разрешения

- **[1] 1k (1920×1920)** - быстро, дешевле
- **[2] 2k (2048×2048)** - баланс качества и скорости
- **[3] 4k (4096×4096)** - максимальное качество, дороже

**Рекомендация:** Начните с 1k для тестирования, затем переходите на 2k или 4k.

### 6.7 Генерация captions

**Вопрос:** "Генерировать подписи (captions) для LoRA обучения?"

- **[1] Да** → Создаст .txt файлы для каждого изображения
- **[2] Нет** → Пропустить генерацию captions

**Рекомендация:** Всегда включайте captions для обучения LoRA.

### 6.8 Trigger name (имя персонажа)

Если включили captions, нужно указать **trigger name**:

- Это имя персонажа, которое будет использоваться в каждой подписи
- Примеры: `Elara`, `John`, `CharacterName`
- ⚠️ Используйте одно слово без пробелов

**Пример:** Если trigger name = `Elara`, то в captions будет: `Elara with blonde hair, wearing...`

### 6.9 Выбор провайдера для captions

- **[1] OpenAI** → Автоматически GPT-5.1
- **[2] Grok** → Автоматически grok-4-1-fast-reasoning

**Рекомендации:**
- Для обычного контента: OpenAI (качество)
- Для NSFW контента: автоматически выберется Grok

### 6.10 Итоговый отчет

Перед началом обработки вы увидите сводку всех настроек:

```
✅ Настройки выбраны!

📋 Итоговые настройки:

🔧 Настройки для обычного контента:
   AI провайдер: gemini
   Wavespeed модель: nano-banana-pro/edit
   ...

🔞 Настройки для NSFW и обычного контента:
   📌 NSFW контент:
      AI провайдер: grok
      ...
```

Проверьте настройки и подтвердите.

### 6.11 Сохранение профиля

В конце настройки спросят, сохранить ли настройки как профиль:

- **[1] Да** → Введите имя профиля (например, `production`, `testing`)
- **[2] Нет** → Настройки используются только для этого запуска

**Рекомендация:** Сохраняйте профили для разных задач.

---

## 📊 Шаг 7: Процесс обработки

### 7.1 Что происходит во время обработки

Скрипт автоматически:

1. **Загружает референсные изображения** (минимум 2)
2. **Загружает sample изображения** из выбранных папок
3. **Для каждого sample изображения:**
   - Генерирует промпт через выбранный AI провайдер
   - Создает новое изображение через Wavespeed API
   - Сохраняет промпт в файл `*_prompt.txt`
   - Сохраняет изображение в `output/lora_dataset/`
4. **Генерирует captions** (если включено)
5. **Создает zip-архив** с готовым датасетом

### 7.2 Прогресс обработки

Вы увидите в консоли:

```
🖼️  Обработка 1/10: scene_1.jpg
   📌 Используются настройки для обычного контента
   🤖 Генерация промпта...
   ✓ Промпт сгенерирован (4190 символов)
   🎨 Генерация изображения через wavespeed...
   ✓ Изображение сохранено: output/lora_dataset/test_0001.png
```

### 7.3 Результаты

После обработки все файлы сохраняются в:

```
output/
└── lora_dataset/
    ├── trigger_name_0001.png
    ├── trigger_name_0001.txt
    ├── trigger_name_0002.png
    ├── trigger_name_0002.txt
    └── ...
```

И создается zip-архив:
```
output/
└── trigger_name_lora_dataset.zip
```

---

## 💡 Полезные советы и рекомендации

### Совет 1: Начните с малого

Для первого теста:
- Используйте 2-3 sample изображения
- Выберите разрешение 1k (быстрее и дешевле)
- Используйте Gemini (бесплатно)

### Совет 2: Качество референсных изображений

- Используйте четкие фотографии лица
- Разные ракурсы улучшают результат
- Минимум 2 изображения, лучше 3-5

### Совет 3: Экономия средств

- Используйте Gemini для промптов (бесплатно)
- Начните с разрешения 1k
- Используйте Seedream v4 вместо v4.5 (дешевле на 33%)

### Совет 4: Профили для разных задач

Создайте несколько профилей:
- `testing` - для тестов (1k, Gemini, без captions)
- `production` - для финального датасета (2k/4k, OpenAI, с captions)
- `nsfw` - для NSFW контента (Grok, Seedream v4.5)

### Совет 5: Организация файлов

```
makenanalog/
├── Influencer Reference Images/
│   ├── character1_face1.jpg
│   ├── character1_face2.jpg
│   ├── character2_face1.jpg
│   └── character2_face2.jpg
├── Sample Dataset/
│   ├── normal/
│   │   └── scenes/
│   └── nsfw/
│       └── scenes/
└── output/
    └── lora_dataset/
```

---

## 🔧 Решение проблем

### Проблема: "API ключ не установлен"

**Решение:**
1. Проверьте, что заполнили ключ в `config.json`
2. Убедитесь, что нет лишних пробелов или кавычек
3. Проверьте правильность ключа

### Проблема: "Папка не найдена"

**Решение:**
1. Создайте папки вручную:
   ```bash
   mkdir -p "Influencer Reference Images"
   mkdir -p "Sample Dataset/normal"
   mkdir -p "Sample Dataset/nsfw"
   ```
2. Проверьте пути в `config.json`

### Проблема: "Ошибка при генерации изображения"

**Решение:**
1. Проверьте баланс на Wavespeed
2. Проверьте интернет соединение
3. Посмотрите файлы `*_response.json` в папке output для деталей ошибки

### Проблема: NSFW контент не обрабатывается

**Решение:**
1. Убедитесь, что выбрали "Да" для NSFW контента в меню
2. Проверьте, что добавили `grok_api_key` в `config.json`
3. Убедитесь, что файлы находятся в папке `Sample Dataset/nsfw/`

---

## 📚 Дополнительная информация

### Структура проекта

```
makenanalog/
├── src/                    # Основной код
├── main.py                 # Точка входа (используйте это)
├── config.json             # Ваши настройки и API ключи
├── profiles/               # Сохраненные профили
├── Influencer Reference Images/  # Референсные изображения
├── Sample Dataset/         # Sample изображения
│   ├── normal/            # Обычный контент
│   └── nsfw/              # NSFW контент
└── output/                # Результаты
    └── lora_dataset/      # Готовый датасет
```

### Использование профилей

**Запуск с профилем:**
```bash
python main.py --profile production
```

**Список профилей:**
```bash
python main.py --list-profiles
```

### Примеры использования

**Сценарий 1: Быстрый тест**
1. Используйте Gemini (бесплатно)
2. Разрешение 1k
3. 2-3 sample изображения
4. Без captions (для скорости)

**Сценарий 2: Создание датасета для обучения**
1. Используйте OpenAI (качество)
2. Разрешение 2k или 4k
3. Много sample изображений (bulk режим)
4. С captions и trigger name

**Сценарий 3: NSFW контент**
1. Обязательно Grok API ключ
2. Выберите "Да" для NSFW в меню
3. Автоматически: Grok + Seedream v4.5
4. Файлы в `Sample Dataset/nsfw/`

---

## ✅ Чек-лист перед запуском

Перед первым запуском убедитесь:

- [ ] Python 3.8+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] `config.json` создан из `config.example.json`
- [ ] Добавлен хотя бы один API ключ (Gemini, OpenAI или Grok)
- [ ] Добавлен Wavespeed API ключ
- [ ] Минимум 2 референсных изображения в `Influencer Reference Images/`
- [ ] Хотя бы одно sample изображение в `Sample Dataset/normal/`
- [ ] Если NSFW: добавлен `grok_api_key` и файлы в `Sample Dataset/nsfw/`

---

## 🎉 Готово!

Теперь вы знаете, как использовать Dataset Creation Tool. Начните с простого теста, а затем создавайте полноценные датасеты для обучения LoRA моделей!

**Удачи в создании датасетов! 🚀**

---

## 📞 Нужна помощь?

- Проверьте раздел "Решение проблем" в README
- Изучите логи в консоли
- Проверьте файлы `*_response.json` в папке output для деталей ошибок

